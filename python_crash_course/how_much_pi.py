# how_much_pi
# Determine to what precision this machine returns Pi

import math

pi_string = str(round(math.pi,1000000))

print(len(pi_string))

# only uncomment the next line after running once and confirming you
# want to wait around for that many digits
print(pi_string)
